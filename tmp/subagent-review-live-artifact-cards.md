## Review

- Correct: Final/downloadable artifact cards remain intact. `ArtifactToolCard` still renders ready `output-available` artifacts with view/download actions when `pdf.downloadUrl` exists (`src/components/ai-elements/artifact-tool-card.tsx:93-110`), and the new test covers a completed Field Brief remaining downloadable while a next live artifact card is active (`src/components/chat-interface.test.tsx:343-379`).
- Correct: Live artifact input states now render only when `isLive` is true at the card level; non-live `input-streaming` / `input-available` states return `null` (`src/components/ai-elements/artifact-tool-card.tsx:29-55`). Tests cover both live rendering and stale suppression at the component level (`src/components/chat-interface.test.tsx:304-321`).
- Correct: The generic artifact package heartbeat no longer competes with named artifact cards because `shouldShowArtifactPackageHeartbeat` now always returns `false` (`src/components/chat-interface.tsx:120-125`), with updated test coverage (`src/components/chat-interface.test.tsx:469-490`).
- Correct: AgentStatusProgress no longer hides merely because assistant text exists; it remains visible until a card-visible tool state is detected (`src/lib/chat-utils.ts:55-59`). The updated test covers text plus no tool card still showing status (`src/components/chat-interface.test.tsx:177-193`).
- Correct: Targeted test file passes: `bun run test src/components/chat-interface.test.tsx` → 37 tests passed.

- High: Stale/history live detection can mark a previous assistant as live during a new submitted request before the new assistant message exists. `lastAssistantMessageId` is computed as the last assistant anywhere in the full history (`src/components/chat-interface.tsx:443-445`), and `isLiveAssistant` is true whenever status is `submitted` or `streaming` and the message id matches that historical last assistant (`src/components/chat-interface.tsx:546-549`). If the prior last assistant contains a persisted/incomplete `input-streaming` or `input-available` artifact part, submitting a new user message will temporarily render that old artifact as a live card, violating the intended behavior that live cards show only for the current live assistant. This also creates a stale-history risk specifically called out in the review request.
- Medium: AgentStatusProgress can hide when no concrete card exists because `hasArtifactCardVisible` treats any `tool-*` part in `input-streaming`, `input-available`, `output-available`, or `output-error` as card-visible (`src/lib/chat-utils.ts:37-43`). It does not restrict the check to artifact tool parts or to the currently live assistant. Generic/non-rendered tool parts with those states can suppress AgentStatusProgress even though `ChatInterface` renders no concrete card for them (`src/components/chat-interface.tsx:613-637`). This conflicts with the intended behavior that AgentStatusProgress hides once a concrete card exists.

Ready to proceed: No. The core component behavior and tests are in the right direction, but the stale previous-assistant live-state bug should be fixed before proceeding.
