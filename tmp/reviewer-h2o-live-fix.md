## Review

- Correct: `tool-loadSkill` no longer renders as a visible card. `getToolRenderingMetadata` returns metadata only for artifact tool types and returns `null` for other tools (`src/components/chat-interface.tsx:91-104`), and the render path returns `null` when metadata is absent (`src/components/chat-interface.tsx:586-590`). The focused test covers `tool-loadSkill` hiding (`src/components/chat-interface.test.tsx:168-178`).
- Correct: A single high-level artifact heartbeat was added while artifact tools are active (`src/components/chat-interface.tsx:109-124`, rendered at `src/components/chat-interface.tsx:536-539`). Tests cover active and all-terminal heartbeat behavior (`src/components/chat-interface.test.tsx:206-253`).
- Correct: Long reconciliation defaults are configured at ~300s with capped retry backoff (`src/lib/chat-reconciliation.ts:40-45`), wired into the chat UI (`src/components/chat-interface.tsx:406-412`), and covered by test (`src/components/chat-interface.test.tsx:292-300`).
- Correct: Stale length/tail guards and supersession guards exist in reconciliation: persisted-behind and assistant-tail-missing decisions (`src/lib/chat-reconciliation.ts:96-118`), request supersession before fetch/apply (`src/lib/chat-reconciliation.ts:160-170`, `src/lib/chat-reconciliation.ts:198-205`).
- Correct: Focused tests pass: `bun run test src/components/chat-interface.test.tsx` → 23 passed.

- Blocker: Terminal-artifact reconciliation checks for any terminal artifact anywhere in the persisted thread, not the current/live assistant artifact package. `hasArtifactTool` and `hasTerminalArtifactTool` scan all messages globally (`src/lib/chat-reconciliation.ts:67-73`), and the apply gate only requires `!hasTerminalArtifactTool(freshMessages)` to be false (`src/lib/chat-reconciliation.ts:188-195`). This means a previous completed artifact tool in an older assistant message can satisfy the terminal check for a newer active artifact turn, allowing `setMessages(freshMessages)` (`src/lib/chat-reconciliation.ts:198-201`) even when the current assistant’s artifact tools are still non-terminal. It also allows applying a package snapshot where only one of multiple artifact tools is terminal. This violates the requirement to poll persisted thread messages until the persisted assistant contains terminal artifact tool states and to avoid stale overwrites. The current tests cover only a single active artifact with no prior terminal artifact (`src/components/chat-interface.test.tsx:399-466`, `src/components/chat-interface.test.tsx:468-500`), so this regression path is untested.

- Non-blocking issue: The reconciliation tests do not cover multi-artifact package completion or a thread with an earlier terminal artifact plus a later active artifact. Adding those would directly protect the blocker above.

- Confidence: Medium-high. I reviewed the requested diff/files and ran the focused test file successfully. I did not run full typecheck or full test suite.
